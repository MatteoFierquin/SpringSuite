import { Routes } from '@angular/router';
import { CalendarListComponent } from './pages/calendar-list/calendar-list.component';

export const CALENDAR_ROUTES: Routes = [{ path: '', component: CalendarListComponent }];
